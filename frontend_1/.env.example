# Public backend origin only (do not append /api), for example:
# VITE_API_BASE_URL=https://your-render-service.onrender.com
# Leave empty for local development to use Vite's /api proxy.
VITE_API_BASE_URL=
